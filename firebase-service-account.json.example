# Firebase Admin SDK — download from Firebase Console:
# Project mkd-delivery-21550 → Settings → Service accounts → Generate new private key
# Save the downloaded file as this path (do not commit to git):
FIREBASE_SERVICE_ACCOUNT_PATH=./firebase-service-account.json
